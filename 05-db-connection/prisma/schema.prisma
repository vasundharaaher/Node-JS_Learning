generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
}

model fruits {
  id   Int    @id @default(autoincrement())
  name String @db.Text
  qt   Int
}
